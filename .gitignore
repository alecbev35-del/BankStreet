# macOS
.DS_Store
**/.DS_Store

# Archives of source art (originals live outside the repo)
*.zip

# Scratch
/tmp/

# Booking calendar exports — contain reservation IDs + guest phone digits (PII). Never commit.
bookings/
*.ics
